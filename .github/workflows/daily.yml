name: Daily build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - run: ./.github/assets/build.sh

    - uses: actions/upload-artifact@v2
      with:
        name: build
        path: ./build.tar.gz
